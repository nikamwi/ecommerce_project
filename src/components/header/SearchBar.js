import { Typography } from '@mui/material'
import { color } from '@mui/system'
import React from 'react'

export const SearchBar = () => {
  return (
    <Typography sx={{color:"black"}}>SearchBar</Typography>
  )
}
